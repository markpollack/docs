# Status: lab.pollack.ai Docs (`markpollack/docs`)

> **Period**: 2026-06-08 through 2026-08-13
> **Last updated**: 2026-08-13

## Summary

This repo is the **Mintlify source for `lab.pollack.ai`** (site name "Pollack AI Lab", `docs.json`, auto-deploy on push to `main`, no build step). 26 commits this window in three clusters: a **release-train documentation cascade** (acp-java 0.13/0.14, agentworks-bom 1.8→1.15, agent-journal 1.5/1.6, agent-client 0.24/0.25), a **site restructure** (capability-led home page, per-project What's New pages, blog CTAs), and a **full Agent Judge 0.14 rewrite** (Aug 11–12). Verified live: the site is served from this repo and is **current only through 2026-07-01** — the six Agent Judge commits are committed locally but **not pushed**, so they are not published.

## What Was Accomplished

1. **Release-train documentation cascade** (Jun 9 – Jul 1, 13 commits) — the standard per-release ritual (project card version + blurb, BOM member table, migration snippet, install pins, home-page train chip, What's New entry) run for: acp-java-sdk 0.13.0 → 0.14.0, agentworks-bom 1.8.0 → 1.12.0 → 1.13.0 → 1.14.0 → 1.15.0, agent-journal 1.5.0 (Gemini CLI capture) → 1.6.0, agent-client 0.24.0 → 0.25.0 (auto-config fix). Includes a version-currency reconciliation pass (`3730d0f`) that fixed an agent-journal phantom module and an agent-workflow tutorial inconsistency.

2. **Site restructure** (Jun 11–17, 6 commits) — added **per-project What's New pages** (12 of them, auto-generated from each project's git history) plus nav groups and card links; revamped `index.mdx` into a capability-led **Highlights + active release train** layout; refreshed all project-card blurbs to source from What's New; added Code Coverage v3 to experiments and marked v2 complete; added "Read the Blog" top-nav button and a mobile-visible banner CTA to `blog.pollack.ai`.

3. **Agent Judge 0.14 documentation rewrite** (Aug 11–12, 6 commits — **unpushed**) — the largest single change of the window: 13 files, **+617 / −2584 lines**. Rewrote all 8 Agent Judge doc pages plus the project card around 0.14 (result model, portable evidence, aggregation, API/dependency changes, framework baselines, samples, release packaging); **added a new page `docs/agent-judge/research-foundations.mdx`** with its nav entry; repositioned Agent Judge in `index.mdx` and `about.mdx` from "scores output" to **"verifies whether work satisfies its goal"** (executable checks + LLM/RAG evaluation + explicit jury policy); bumped the home chip agent-judge 0.13.0 → 0.14.0. Two follow-up corrections landed: `c679831` (unknown-classifier outcome) and `9c49be2` (Agent Judge discovery claims). This confirms the sibling reports — `9c49be2` is real and the "9 Pollack AI Lab pages updated" = the 8 rewritten + 1 new page under `docs/agent-judge/`.

## Page Inventory

133 published pages. Site name **Pollack AI Lab**; nav tabs = Projects / Methodology / Experiments.

| Section | Count | Contents |
|---------|-------|----------|
| `projects/` | 20 | Thin overview cards: acp-java-sdk, agent-bench, agent-client, agent-experiment, agent-hooks, agent-journal, **agent-judge**, agent-memory, agent-sandbox, agent-skills, agent-tools, agent-workflow, agentworks-bom, agento-studio, agento-university, bud, claude-agent-sdk, loopy, spring-ai-a2a, index |
| `docs/` | 96 | Deep docs for 14 projects + `migration/` + `glossary.mdx`; includes **12 per-project `whats-new.mdx`** pages (new this window) |
| `methodology/` | 9 | forge, four-tier-jury, improvement-flywheel, knowledge-base-design, knowledge-base-freshness, knowledge-directed-execution, markov-fingerprinting, structured-agent-execution, index |
| `experiments/` | 5 | code-coverage-v1/v2/v3, issue-classification, index |

**Changed this window**: 53 files. Heaviest churn — all 9 `docs/agent-judge/*` pages, 12 `whats-new.mdx` pages (all new), 12 `projects/*.mdx` cards, `index.mdx`, `about.mdx`, `docs.json`, `custom.css`, plus targeted fixes in acp-java-sdk (autoconfig, 4 tutorial pages, java reference), agent-client (getting-started, tutorial 01) and agent-workflow (agent-loop, durability, getting-started, trace-capture, tutorial).

## Publish State — ⚠️ site is behind the repo

- **Live site verified**: `https://lab.pollack.ai/` responds and is served from this repo (home page shows "Active Release Train **1.15.0**", the `255c5ee` state).
- **`main` is 6 commits ahead of `origin/main`.** `origin/main` = `255c5ee` (2026-07-01). Everything from the Agent Judge 0.14 rewrite (Aug 11–12) is unpublished.
- **Confirmed by probe**: `https://lab.pollack.ai/docs/agent-judge/research-foundations` returns **HTTP 404** — the page exists only in the unpushed commits.
- Live site therefore still shows **agent-judge 0.13.0** and the old "Agent Judge scores output" framing.
- **Fix is one command**: `git push` (Mintlify auto-deploys on push to `main`; no CI, no build).
- Untracked in `plans/` only (status files) — nothing else uncommitted.

## Current State

### What's Working
- Auto-deploy path is intact; no build system, no CI to break.
- Per-project What's New pages give each library its own changelog surface, sourced from git history.
- Release-cascade ritual is well-established and consistently applied (card + member table + migration + pins + chip + What's New).

### What's Next
1. **`git push`** — publish the 6 Agent Judge 0.14 commits. Highest-value single action; the flagship project's docs are two months stale on the live site.
2. **Stale home-page chip**: the release-train chip reads `agent-client 0.23.0` while the BOM 1.15.0 member table documents **0.25.0**. Same class of bug as `c1eecf9` ("chip was right, prose lagged"), now inverted.
3. **Site-wide `whats-new.mdx` gap**: latest entry is "Week of June 29, 2026". No July or August entries — Agent Judge 0.14 was recorded only in `docs/agent-judge/whats-new.mdx`, not the site-wide page.
4. Consider a version-currency sweep (as in `3730d0f`) covering the July/August releases.

## Where to Look for Details
| Document | Path | What It Contains |
|----------|------|-----------------|
| Site config / nav | `docs.json` | Mintlify v4 config: nav tabs, colors, navbar, blog banner |
| Home page | `index.mdx` | Capability-led Highlights + release-train chips (agent-client chip stale) |
| Site-wide changelog | `whats-new.mdx` | Weekly release notes; ends at Week of June 29 |
| Agent Judge docs | `docs/agent-judge/` | 9 pages, all rewritten for 0.14 (unpushed) |
| New page | `docs/agent-judge/research-foundations.mdx` | Added in the 0.14 rewrite; 404s on the live site |
| Project cards | `projects/` | 20 thin overview pages linking into `docs/` |
| Writing conventions | `.agent/documentation-framework.md` | Diataxis framework, per-project doc standards (unpublished) |

## Cross-Project Impact
| Project | What Changed | Where |
|---------|-------------|-------|
| agent-judge | 0.14 release documented end-to-end — the source of `9c49be2` and the "9 pages updated" report from agent-judge / agent-judge-steward | `docs/agent-judge/*`, `projects/agent-judge.mdx` |
| agentworks-bom | BOM 1.8 → 1.15 cards, member tables, migration snippets | `docs/agentworks-bom/`, `projects/agentworks-bom.mdx` |
| agent-client | 0.24.0 / 0.25.0 docs + install pins (auto-config fix note) | `docs/agent-client/`, `projects/agent-client.mdx` |
| agent-journal | 1.5.0 (Gemini CLI capture) / 1.6.0 | `docs/agent-journal/`, `projects/agent-journal.mdx` |
| acp-java-sdk | 0.13.0 / 0.14.0 docs, tutorial + autoconfig fixes | `docs/acp-java-sdk/` |
| agent-workflow | Tutorial/durability/trace-capture consistency fixes | `docs/agent-workflow/` |
| experiment-code-coverage-v3 | Added to the experiments index; v2 marked complete | `experiments/`, `index.mdx` |
| blog.pollack.ai | Two CTAs added (top-nav button + site banner) driving traffic to the blog | `docs.json`, `custom.css` |

## Notes — Repo Hygiene

- **No build file or `src/` tree** (Mintlify content-only), so no `## Federation Routing` block is emitted.
- **`CLAUDE.md` is stale** (last commit 2026-04-10, before this entire window). Two concrete errors: it names **`mint.json`** as the config file (the repo has used `docs.json` since the Mintlify v4 migration), and it lists **`Local: ~/projects/pollack-ai-lab/`** as this project's directory — that is a *different, older* repo (`markpollack/pollack-ai-lab`, last commit 2026-04-21, still on `mint.json`, has uncommitted edits) and is not what publishes `lab.pollack.ai`. Flagged only; not edited from this report.
