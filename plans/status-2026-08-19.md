# Status: lab.pollack.ai Docs (`markpollack/docs`)

> **Period**: 2026-08-13 through 2026-08-19
> **Last updated**: 2026-08-19

## Summary

The Mintlify source for **`lab.pollack.ai`** (site name "Pollack AI Lab", `docs.json`, auto-deploy on
push to `main`, no build step). **18 commits** this window, and the headline is that the 2026-08-13
publish blockage **cleared**: the Agent Judge 0.14 rewrite that was sitting in 6 unpushed commits is
now on `origin/main` and **live** (`/docs/agent-judge/research-foundations` returns **HTTP 200**, was
404). Agent Judge has since moved on to **0.15.0**. The rest of the window is a large multi-project
release-doc cascade (Agent Journal 1.7.0, Agent Experiment 0.6.0, Agent Bench 0.6.0, Agent Sandbox
0.10.0, Agent Memory 0.4.0, Agent Client 0.26.0) plus a site-wide broken-link repair. The `docs/CLAUDE.md`
pointer trap (`~/projects/pollack-ai-lab`) was **fixed at source** on 2026-08-16.

## Publish State — repo published, live site current

- **`origin/main` = `ad4d406`** ("Document Agent Judge 0.15.0", 2026-08-19). **Nothing is unpushed on `main`.**
- **Local checkout `~/projects/docs` is `main` @ `7dd6788`, 13 behind `origin/main`, 0 ahead.** The
  divergence is a *stale local working copy only* — no local-only work is at risk. A plain
  `git -C ~/projects/docs pull --ff-only` resolves it. (The 12-behind noted at fetch time is now 13
  after `ad4d406`.)
- **Live-site probes (2026-08-19)** — all HTTP 200 and version-current:
  | Page | Status | Versions rendered |
  |------|--------|-------------------|
  | `/docs/agent-judge/research-foundations` | **200** (was 404 on 08-13) | — |
  | `/docs/agent-judge/whats-new` | 200 | 0.14.0, **0.15.0** |
  | `/docs/agent-bench/getting-started` | 200 | 0.6.0 |
  | `/docs/agent-sandbox/whats-new` | 200 | 0.10.0 |
  | `/docs/agent-memory/whats-new` | 200 | 0.4.0 |
  | `/whats-new` (site-wide) | 200 | 0.4.0, 0.6.0, 0.10.0, 0.14.0, 1.7.0 |
  The site reflects the newest commit (`ad4d406`, same day), so **auto-deploy is confirmed healthy**.

### Remaining divergence — 3 unpushed commits, all on side branches (not `main`)

Work parked in worktrees, none of it published:

| Branch (worktree) | Commit | Content |
|---|---|---|
| `diligence/docs-agent-hooks-20260818` | `ef08511` | Agent Hooks card correction + qualified 0.7.0 section |
| `diligence/docs-claude-agent-sdk-1.5.0-20260819` | `f7643f6` | Claude Agent SDK doc corrections, 1.5.0 staged **as unreleased** |
| `diligence/docs-claude-agent-sdk-1.5.0-20260818` | `c75ea0c` | same change, superseded duplicate of `f7643f6` |
| `diligence/agent-bench-no-containers-20260818` | `a3e236d` | superseded — equivalent landed as `41b4f2d` on main |

These are **deliberate holds** (Agent Hooks 0.7.0 and Claude Agent SDK 1.5.0 are not released), the
same correct sequencing that held the 0.14 docs. 12 worktrees are currently open against this repo —
worth a prune pass once the holds clear.

## What Was Accomplished

1. **Agent Judge 0.14 published, then 0.15.0 documented** (`594d65b` 08-16, `ad4d406` 08-19) — the
   6-commit 0.14 rewrite (8 pages + new `research-foundations.mdx`) reached `origin/main`; `594d65b`
   added 0.14 to the **site-wide** What's New; `ad4d406` documents **0.15.0**. The KEY QUESTION from
   the 08-13 sweep is closed: pushed and live.

2. **Six-project release-doc cascade** (08-17 → 08-19, ~9 commits) — Agent Client 0.26.0 (`98460bd`),
   Agent Journal 1.7.0 (`c6a94f1`), Agent Experiment 0.6.0 (`be7c8ad`), Agent Bench 0.6.0 (`003e7d6`),
   Agent Memory 0.4.0 (`885c738`, dated by `54be6e7` — AM-17), Agent Sandbox 0.10.0 (`b77aa69`).
   Standard ritual each time: project card + version pin + What's New entry + home-page release chip.

3. **Agent Journal reference consolidation** (`06abd8e`, `7dd6788`) — 5 new deep pages
   (`getting-started`, `concepts`, `capture-sessions`, `analyzing-runs`, `api-reference`, +712 lines)
   extracted from an overloaded `projects/agent-journal.mdx` card (−205 lines), with `docs.json` nav
   rewired; `7dd6788` then corrected the version history and layout.

4. **Accuracy corrections — boundary claims** (`884cea0`, `41b4f2d`) — Agent Sandbox runtime boundaries
   and Agent Bench's **local-execution / no-containers** boundary. Continues the 0.14-era pattern of
   walking back over-broad capability claims in published docs.

5. **Site-wide link and asset repair** (`74e0776`, `b1c8715`) — 31 files, 41 link fixes across
   acp-java-sdk tutorials, agent-client reference, methodology and project cards; restored the missing
   `images/acp-protocol.svg`.

6. **`CLAUDE.md` canonical-checkout fix** (`fcf8900`, 08-16) — **the trap is fixed at source**. `**Local**:
   ~/projects/pollack-ai-lab/` replaced with `**Canonical local checkout**: ~/projects/docs/` plus an
   explicit warning that `~/projects/pollack-ai-lab/` is "a different historical repository."

7. **LangChain4j compatibility matrix → 1.19.0** (`f7cbb76`, 08-14).

## Known Doc Defects (unfixed)

- **`CLAUDE.md` still names `mint.json`** at lines 36 and 51 — the file does not exist; the repo uses
  `docs.json`. Half of the 08-13 registry warning remains true. One-line fix, not applied.

## Current State

### What's Working
- Push → deploy path verified end-to-end on the same day; no CI, no build to break.
- Per-project `whats-new.mdx` surfaces are keeping pace with the release train.
- The "hold docs until the release ships" discipline is being applied consistently (0.14 then, Hooks
  0.7.0 / Claude SDK 1.5.0 now).

### What's Next
- `git -C ~/projects/docs pull --ff-only` — realign the stale local checkout (13 behind).
- Publish `ef08511` (Agent Hooks 0.7.0) and `f7643f6` (Claude Agent SDK 1.5.0) when those releases land.
- Delete superseded branches/worktrees `a3e236d` and `c75ea0c`; prune the 12 open worktrees.
- Fix the residual `mint.json` → `docs.json` error in `CLAUDE.md`.

## Page Inventory

**127 pages** in the `docs.json` nav (140 `.mdx`/`.md` content files tracked, excluding `plans/` and
`.agent/`) — down from the 133 recorded on 08-13, consistent with the Agent Journal consolidation and
nav rewiring. Tabs unchanged: Projects / Methodology / Experiments.

**Window churn**: 71 files, **+1,452 / −783**. 6 new files (5 agent-journal pages + `acp-protocol.svg`),
0 deletions.

## Where to Look for Details
| Document | Path | What It Contains |
|----------|------|-----------------|
| Prior status | `plans/status-2026-08-13.md` | The 6-unpushed-commit finding this report closes |
| Nav / config | `docs.json` | 127-page navigation (not `mint.json`) |
| Judge docs | `docs/agent-judge/` | 9 pages incl. `research-foundations.mdx` (now live) |
| Journal docs | `docs/agent-journal/` | 6 pages, consolidated this window |
| Writing standards | `.agent/documentation-framework.md` | Diataxis + per-project doc standards |
| Entry point | `CLAUDE.md` | Fixed checkout pointer; still wrong on `mint.json` |

> Paths above are verified against **`origin/main`**. `docs/agent-journal/*` and `images/acp-protocol.svg`
> are not present in the local working tree yet — it is 13 commits behind; they appear after a pull.

## Cross-Project Impact
| Project | What Changed | Where |
|---------|-------------|-------|
| agent-judge | 0.14 docs published live; 0.15.0 documented | `docs/agent-judge/*`, `whats-new.mdx` |
| agent-journal | 1.7.0 release docs + reference consolidation | `docs/agent-journal/*` |
| agent-experiment | 0.6.0 release docs | `docs/agent-experiment/whats-new.mdx` |
| agent-bench | 0.6.0 docs + local-execution boundary correction | `docs/agent-bench/*` |
| agent-sandbox | 0.10.0 docs + runtime-boundary correction | `docs/agent-sandbox/whats-new.mdx` |
| agent-memory | 0.4.0 docs (AM-17) | `docs/agent-memory/whats-new.mdx` |
| agent-client | 0.26.0 docs | `docs/agent-client/*` |
| acp-java-sdk | Link repairs + protocol diagram restored | `docs/acp-java-sdk/*`, `images/acp-protocol.svg` |
| claude-agent-sdk | 1.5.0 docs staged, **held unpublished** | branch `diligence/docs-claude-agent-sdk-1.5.0-20260819` |
| agent-hooks | 0.7.0 section staged, **held unpublished** | branch `diligence/docs-agent-hooks-20260818` |
| langchain4j (ext) | Compatibility matrix → 1.19.0 | agent-judge docs |

## Federation Routing

- **Declared entry point**: `CLAUDE.md` (registry `kb/projects.yaml`, id `docs`)
- **Entry doc last commit**: 2026-08-16 (`fcf8900`) — 15 substantive commits since
- **Latest status report**: this file
- **Stale docs detected**: `CLAUDE.md` — still names `mint.json` (file absent; repo uses `docs.json`)
- **Drift**: yes (minor)
- **Recommended routing**: keep `CLAUDE.md`
- **Confidence**: high
- **Reason**: The serious half of the drift (the `~/projects/pollack-ai-lab` mis-pointer) was fixed in
  `fcf8900`; only the `mint.json`/`docs.json` naming error remains, which misroutes config edits but
  not repo choice. `CLAUDE.md` otherwise correctly describes the deploy path, structure, and conventions.

## Build Status

Not applicable — content project, no build system, no CI. Per instruction, no builds run.
